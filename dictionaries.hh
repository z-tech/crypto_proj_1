#ifndef DICTIONARIES_H
#define DICTIONARIES_H

#include <string>
#include <vector>
#include <unordered_map>

extern std::vector<std::string> dict1;
extern std::unordered_map<std::string, bool> dict1Map;
extern std::vector<std::string> words1;

#endif
